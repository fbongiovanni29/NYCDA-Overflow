<% @posts.each do |post| %>
	<div class="post_wrapper">
		<h2 class="title"><%= link_to post.title, user_post_path(current_user.id, post.id) %></h2>

		<p class="date"><%= post.created_at.strftime("%B, %d, %Y") %></p>
	</div>
<% end %>